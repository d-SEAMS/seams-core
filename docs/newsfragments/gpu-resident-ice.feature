The optional gpulite path keeps a batch of frames on the device and
runs the cell list, mutual four-nearest graph, one Steinhardt
:math:`q_{lm}` write, CHILL, primitive six-rings and the HC/DDC
affiliation predicates there. Only the labels come back.

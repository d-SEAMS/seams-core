[workspace]
name = "seams-core"
version = "2.0.0"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-64", "osx-arm64"]
authors = ["Rohit Goswami <rgoswami@ieee.org>"]

[dependencies]
compilers = ">=1.9.0,<2"
eigen = "3.4.0.*"
libblas = ">=3.11.0,<4"
liblapack = ">=3.11.0,<4"
catch2 = ">=3.6.0,<4"
meson = ">=1.2.0"
ninja = ">=1.13.2,<2"
pkgconfig = ">=1.5.5,<2"
python = ">=3.10"
nanobind = ">=2.0"
numpy = ">=1.24"
pip = "*"
meson-python = ">=0.16"
pytest = ">=8.0"

[tasks]
setup = "meson setup bbdir --prefix=$CONDA_PREFIX -Dwith_tests=true -Dwith_python=true --wipe 2>/dev/null; meson setup bbdir --prefix=$CONDA_PREFIX -Dwith_tests=true -Dwith_python=true"
build = { cmd = "ninja -j4 -C bbdir", depends-on = ["setup"] }
test = { cmd = "meson test -C bbdir -v", depends-on = ["build"] }
install = { cmd = "meson install -C bbdir", depends-on = ["build"] }
pytest = { cmd = "pytest tests/python/ -v", depends-on = ["install"] }

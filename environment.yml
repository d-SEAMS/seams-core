# To use:
#
#   $ micromamba create -f environment.yml
#   $ micromamba activate dseams
#
name: dseams
channels:
  - conda-forge
dependencies:
  - compilers
  - gfortran
  - catch2
  - liblapack
  - libblas
  - boost-cpp
  - cmake
  - meson
  - cmake
  - pkgconfig
  # Pinned
  - fmt==9.1.0
  - eigen==3.4.0

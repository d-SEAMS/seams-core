# To use:
#
#   $ micromamba create -f environment.yml
#   $ micromamba activate dseams
#
name: dseams
channels:
  - conda-forge
dependencies:
  - compilers
  - gfortran
  - catch2
  - yaml-cpp
  - liblapack
  - libblas
  - boost-cpp
  - cmake
  - meson
  - cmake
  - pkgconfig
  # - lua-luafilesystem
  - luarocks
  # Pinned
  - lua==5.3.*
